#BSUB -L /bin/bash
#BSUB -q normal
#BSUB -n 8
#BSUB -R 'rusage[mem=16000]'
#BSUB -e ~/Documents/%J/errors.err
#BSUB -o ~/Documents/%J/outputs.out

cd ~/Documents/hERV_project

REF_ANNOTATION_LOC=${REF_ANNOTATION_LOC}
REF_GENOME_LOC=${REF_GENOME_LOC}
hERV_DIR=${hERV_DIR}
hERV_FILE=${hERV_FILE}
COMB_ANNOTATION=${COMB_ANNOTATION}
ANALYSIS_STEP=${ANALYSIS_STEP}
PLATFORM=${PLATFORM}
PAIR_FILE=${PAIR_FILE}
SOURCE_LOC=${SOURCE_LOC}
CLEAR_TMP=${CLEAR_TMP}
OVER_WRITE=${OVER_WRITE}
COUNT_METHOD=${COUNT_METHOD}
BATCH_NAME=${BATCH_NAME}

bash analysis.sh ${batch_name}